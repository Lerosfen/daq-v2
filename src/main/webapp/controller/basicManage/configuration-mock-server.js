/** * configuration获取 *分页数据 */Mock.mock(wwwroot+'/basic/configuration',function(options) {    var result = {        total: 580,        rows:[]    };    var offsetNum = getPostParam(options).offset;    for(var i = 0 ; i < 20 ; i++){        result.rows.push(Mock.mock({            "id":/[a-z][A-Z][0-9]/,            "confName":"配置"+(offsetNum+i),            "confCode":/[a-z][A-Z][0-9]/,            "confDesc":"配置" + (offsetNum+i)  + "配置信息描述",            "confValue" : /[a-z][A-Z][0-9]/,            "createTime":Mock.Random.datetime(),            "modifyTime":Mock.Random.datetime()        }));    };    return result;});//根据ID获取字典信息Mock.mock(RegExp(wwwroot + '/basic/configuration'+ ".*"),"get",function() {    return {        code:"code_200",        msg:"查询成功！",        data:Mock.mock({            "id":/[a-z][A-Z][0-9]/,            "confName":"配置X",            "confCode":/[a-z][A-Z][0-9]/,            "confDesc":"配置信息描述",            "confValue" :/[a-z][A-Z][0-9]/,            "createTime":Mock.Random.datetime(),            "modifyTime":Mock.Random.datetime()        })    };});